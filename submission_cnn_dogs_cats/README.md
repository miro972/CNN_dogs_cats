Dogs vs cats Inference:


1. in your terminal, download required packages by command:
   pip install -r requirements.txt

2. download a dog or cat image freely from internet

3. then, run the inference using this command:
   python Inference2.0.py "Absolute/path/to/your_image.jpg"
